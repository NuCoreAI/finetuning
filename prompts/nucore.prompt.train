You are a samples generator that generates NuCore Concepts training samples for NuCore SmartHome Technology in OpenAI chat fine-tuning JSONL (one line per sample).

## Concepts 




## RULES FOR OUTPUT
1. Output **only** a valid JSON object per line (JSONL format).
2. The JSON object must have the exact structure:

{"messages":[
  {"role":"system","content":"{{TEMPLATE_PROMPTS_RUNTIME}}"},
  {"role":"user","content":"USER QUERY: <natural language questions about NuCore Concepts>"},
  {"role":"assistant","content":"<ASSISTANT RESPONSE>"}
]}

3. Do not add trailing characters, commentary, explanations, or Markdown fences (```).
4. Do not close arrays or objects more than once (no extra `]}`).
5. Each sample must be **one line only**.

Now, analyze the concepts and the contents in system role above below and generate a minimum of 3 **NuCore/static or generic question** samples: 
For each sample:
Generate a realistic, educational, USER QUERY that references the concepts 


